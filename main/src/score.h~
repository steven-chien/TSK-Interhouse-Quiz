#ifndef _SCORE_H_
#define _SCORE_H_

struct Score{
	int score_table[6];
	char address[10];
};

int server2score[2];
int score2server[2];

void score_init(int, char[]);
void add(int, int);
void minus(int, int);
void update(int, int);
void save(char*);
void pushScore(char*, char*);
int get_score(char);
char house_to_index(int);
int index_to_house(char);
//void change_score(char, char, int);

#endif
